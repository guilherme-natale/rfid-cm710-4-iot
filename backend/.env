# RFID Cloud IoT System
# DO NOT add secrets here - this file is for non-sensitive config only

# MongoDB Connection (Required)
MONGO_URL=mongodb://localhost:27017
DB_NAME=rfid_cloud

# JWT Configuration
JWT_SECRET=change-this-in-production-use-openssl-rand-hex-32
JWT_EXPIRATION_HOURS=24

# Admin API Key (Required for device registration)
ADMIN_API_KEY=change-this-admin-key-in-production

# RabbitMQ Configuration (for device config distribution)
RABBITMQ_HOST=localhost
RABBITMQ_PORT=5672
RABBITMQ_USER=rfid_user
RABBITMQ_PASSWORD=rfid_password

# CORS Origins (comma-separated)
CORS_ORIGINS=*
